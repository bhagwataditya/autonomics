% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/7_impute.R
\name{impute}
\alias{impute}
\alias{impute.numeric}
\alias{impute.matrix}
\alias{impute.SummarizedExperiment}
\title{Impute}
\usage{
impute(x, ...)

\method{impute}{numeric}(x, shift = 2.5, width = 0.3, verbose = TRUE, plot = FALSE)

\method{impute}{matrix}(
  x,
  shift = 2.5,
  width = 0.3,
  verbose = TRUE,
  plot = FALSE,
  n = min(9, ncol(x)),
  palette = make_colors(colnames(x))
)

\method{impute}{SummarizedExperiment}(
  x,
  shift = 2.5,
  widt = 0.3,
  verbose = TRUE,
  plot = FALSE,
  palette = make_colors(colnames(x))
)
}
\arguments{
\item{x}{numeric vector, matrix, SumExp}

\item{shift}{number: sd units}

\item{width}{number: sd units}

\item{verbose}{TRUE or FALSE}

\item{plot}{TRUE or FALSE}

\item{n}{number of samples to plot}

\item{palette}{color vector}
}
\value{
numeric vector, matrix or SumExp
}
\description{
Impute NA values
}
\details{
Imputes NA values from N(mean - 2.5 sd, 0.3 sd)
}
\examples{
file <- download_data('fukuda20.proteingroups.txt')
object <- read_proteingroups(file)
values(object)[1:10, ]

y <- impute(values(object)[, 1], plot = TRUE)  # vector
y <- impute(values(object), plot = TRUE)       # matrix
y <- impute(object, plot = TRUE)               # sumexp
}
