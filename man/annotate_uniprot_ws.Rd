% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/3_read_proteingroups.R
\name{annotate_uniprot_ws}
\alias{annotate_uniprot_ws}
\alias{annotate_uniprot_ws.data.table}
\alias{annotate_uniprot_ws.SummarizedExperiment}
\title{Annotate uniprotids using UniProt.ws}
\usage{
annotate_uniprot_ws(x, ...)

\method{annotate_uniprot_ws}{data.table}(x, upws, columns = c("ENSEMBL"), collapse = ";", ...)

\method{annotate_uniprot_ws}{SummarizedExperiment}(x, upws, columns = c("ENSEMBL"), collapse = ";", ...)
}
\arguments{
\item{x}{data.table/SummarizedExperiment with (s)var "uniprot" \cr
with single (data.table) or collapsed (SummarizedExperiment) uniprot values}

\item{...}{used for S3 generic definition}

\item{upws}{return value of Uniprot.ws::Uniprot.ws()}

\item{columns}{subset of UniProt.ws::columns(up)}

\item{collapse}{string: used in paste(collapse = .)}
}
\value{
SummarizedExperiment/data.table
}
\description{
Annotate uniprotids in data.table or SummarizedExperiment
}
\details{
data.table: column "uniprot" with uniprotid values (e.g. "Q5JTY5"). \cr

SummarizedExperiment:  svar "feature_id" with collapsed uniprot values for 
a single protein ("A0A068F9P7") or a proteingroup ("A0A068F9P7;F1Q7I4").
On these the mapping is performed. 1:many mappings are collapsed and only then returned.
}
\examples{
# data.table
    x <- data.table::data.table(uniprot = c('A0A068F9P7', 'Q7ZVA2'))
    # upws <- UniProt.ws::UniProt.ws(taxId=7955)
    # annotate_uniprot_ws(x, upws)

# SummarizedExperiment
    require(magrittr)
    file <- download_data('fukuda20.proteingroups.txt')
    x <- read_proteingroups(file, plot=FALSE)
    x \%<>\% extract(1:10, )
    fdata(x)[1:3, ]
    # x \%<>\% annotate_uniprot_ws(upws)
    # fdata(x)[1:3, ]
}
