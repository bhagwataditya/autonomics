% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/8_fit_lmx.R
\name{block_formula_lme}
\alias{block_formula_lme}
\alias{block_formula_lmer}
\alias{block_vars}
\title{Switch blockvar format}
\usage{
block_formula_lme(block, formula, verbose = TRUE)

block_formula_lmer(formula, block, verbose = TRUE)

block_vars(formula)
}
\arguments{
\item{block}{block: charactervector or formula}

\item{formula}{formula}

\item{verbose}{TRUE or FALSE}
}
\description{
Switch blockvar format
}
\examples{
# lme
   block_formula_lme( block = c('subject', 'batch'),           formula = ~ subgroup)
   block_formula_lme( block = list(subject = ~1, batch = ~1),  formula = ~ GROUP + SUBGROUP)
   block_formula_lme( block = ~1|SUB,                          formula = ~ subgroup)
# lmer
   block_formula_lmer(block = c('SUB', 'SEX'), formula = ~ GROUP + SUBGROUP)
   block_formula_lmer(formula = ~GROUP + SUBGROUP + (1|SUB) + (1|SEX))
   block_vars(formula = ~GROUP + SUBGROUP + (1|SUB) + (1|SEX))
}
