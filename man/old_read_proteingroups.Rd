% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/3.2_old_read_proteingroups.R
\name{old_read_proteingroups}
\alias{old_read_proteingroups}
\title{Read/Analyze proteingroups/phosphosites}
\usage{
old_read_proteingroups(
  proteinfile,
  fastafile = NULL,
  quantity = guess_maxquant_quantity(proteinfile),
  select_subgroups = NULL,
  invert_subgroups = character(0),
  contaminants = FALSE,
  reverse = FALSE,
  impute = stri_detect_regex(quantity, "[Ii]ntensity"),
  subgroupvar = NULL,
  formula = NULL,
  block = NULL,
  coefs = NULL,
  contrastdefs = NULL,
  pca = TRUE,
  fit = "limma",
  verbose = TRUE,
  plot = pca & !is.null(fit),
  feature_id = NULL,
  sample_id = NULL,
  palette = NULL
)
}
\arguments{
\item{proteinfile}{proteingroups file}

\item{fastafile}{NULL or fastafile (to deconvolute proteingroups)}

\item{quantity}{string: "Ratio normalized",
"Ratio",
"LFQ intensity",
"Reporter intensity corrected",
"Reporter intensity",
"Intensity labeled",
"Intensity"}

\item{select_subgroups}{subgroups to be selected (character vector)}

\item{invert_subgroups}{subgroups to be inverted (character vector)}

\item{contaminants}{whether to return contaminants}

\item{reverse}{whether to return reverse peptides}

\item{impute}{whether to impute consistent nondetects}

\item{subgroupvar}{subgroup svar}

\item{formula}{desgnmat formula}

\item{block}{block svar}

\item{coefs}{NULL or character vector: model coefficients to test}

\item{contrastdefs}{NULL or character vector: coefficient contrasts to test}

\item{pca}{whether to pca}

\item{fit}{fit model: NULL, 'limma', 'lm', 'lme', 'lmer','wilcoxon'}

\item{verbose}{whether to message}

\item{plot}{whether to plot}

\item{phosphofile}{proteingroups file}

\item{sfile}{sample file}

\item{by.x}{`file`  column to merge sdata: string}

\item{by.y}{`sfile` column to merge sdata: string}

\item{localization}{min site localization probability (number)}

\item{pepcountpattern}{value in MAXQUANT_PATTERNS_PEPCOUNTS}
}
\value{
SummarizedExperiment
}
\description{
Read/Analyze proteingroups/phosphosites
}
\examples{
# Fukuda 2020
    proteinfile <- download_data('fukuda20.proteingroups.txt')
    object <- read_proteingroups(proteinfile, pca=TRUE, fit='limma')
# Billing 2019
    proteinfile <- download_data('billing19.proteingroups.txt')
    phosphofile <- download_data('billing19.phosphosites.txt')
    subgroups <- c('E00', 'E01', 'E02', 'E05', 'E15', 'E30', 'M00')
    subgroups \%<>\% paste0('_STD')
    palette <- make_colors(c(subgroups[1], 'E00.8_STD', subgroups[-1]))
    pro <- read_proteingroups(
               proteinfile, select_subgroups = subgroups, palette = palette, 
               coefs = 'subgroupE05_STD', sample_id = 'E05_STD.R2')
    fos <- read_phosphosites(phosphofile, proteinfile, 
               select_subgroups = subgroups)
}
