% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/8_fit.R
\name{p}
\alias{p}
\alias{pvar}
\alias{effect}
\alias{effectvar}
\alias{fdr}
\alias{fdrvar}
\alias{up}
\alias{down}
\title{Get p/fdr/effect/down/up/sign matrix}
\usage{
p(object, coef = coefficients(object), fit = fits(object))

pvar(object, coef = coefficients(object), fit = fits(object))

effect(object, coef = coefficients(object), fit = fits(object))

effectvar(object, coef = coefficients(object), fit = fits(object))

fdr(object, coef = coefficients(object), fit = fits(object))

fdrvar(object, coef = coefficients(object), fit = fits(object))

up(
  object,
  coef = coefficients(object),
  fit = fits(object),
  var = "fdr",
  cutoff = 0.05
)

down(
  object,
  coef = coefficients(object),
  fit = fits(object),
  var = "fdr",
  cutoff = 0.05
)
}
\arguments{
\item{object}{SummarizedExperiment}

\item{coef}{string}

\item{fit}{string}
}
\value{
matrix (feature x coef)
}
\description{
Get p/fdr/effect/down/up/sign matrix
}
\examples{
# Read
    file <- download_data('atkin18.metabolon.xlsx')
    object <- read_metabolon(file, impute = TRUE, plot = FALSE)
    object \%<>\% fit_limma(subgroupvar = 'SET')
    object \%<>\% fit_lm(   subgroupvar = 'SET')
# Values
    effect(object)[1:3, ]
         p(object)[1:3, ]
       fdr(object)[1:3, ]
      down(object)[1:3, ]
        up(object)[1:3, ]
      sign(object)[1:3, ]
# Variables
    effectvar(object)
         pvar(object)
       fdrvar(object)
}
