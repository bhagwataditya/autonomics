% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/3_plot.R
\name{plot_densities}
\alias{plot_densities}
\alias{plot_sample_densities}
\alias{plot_feature_densities}
\title{Plot sample/feature densities}
\usage{
plot_densities(
  object,
  group,
  fill,
  color = NULL,
  fixed = list(alpha = 0.5, na.rm = TRUE)
)

plot_sample_densities(
  object,
  fill = subgroup,
  color = NULL,
  group = sample_id,
  fixed = list(alpha = 0.5, na.rm = TRUE)
)

plot_feature_densities(
  object,
  fill = feature_id,
  color = NULL,
  group = feature_id,
  fixed = list(alpha = 0.5, na.rm = TRUE)
)
}
\arguments{
\item{object}{SummarizedExperiment}

\item{group}{svar mapped to group}

\item{fill}{svar mapped to fill}

\item{color}{svar mapped to color}

\item{fixed}{fixed aesthetics}
}
\value{
ggplot object
}
\description{
Plot sample/feature densities
}
\examples{
# Read data
    require(magrittr)
    file <- download_data('halama18.metabolon.xlsx')
    object <- read_metabolon(file, plot = FALSE)
    object \%<>\% extract(, order(.$subgroup))
# Plot sample distributions
    plot_sample_densities(object)
    plot_feature_densities(object[1:9, ])
}
\seealso{
\code{\link{plot_sample_violins}}, \code{\link{plot_sample_boxplots}}
}
