% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/8_plot_volcano.R
\name{plot_volcano}
\alias{plot_volcano}
\title{Plot volcano}
\usage{
plot_volcano(
  object,
  fit = fits(object),
  coef = default_coef(object, fit[1]),
  shape = if ("imputed" \%in\% fvars(object)) "imputed" else NULL,
  size = NULL,
  alpha = NULL,
  label = "feature_id",
  max.overlaps = 10,
  features = NULL,
  nrow = length(fit)
)
}
\arguments{
\item{object}{SummarizedExperiment}

\item{fit}{'limma', 'lme', 'lm', 'wilcoxon'}

\item{coef}{character vector}

\item{shape}{shape fvar (string)}

\item{size}{size  fvar (string)}

\item{alpha}{alpha fvar (string)}

\item{label}{label fvar (string)}

\item{max.overlaps}{number: passed to ggrepel}

\item{features}{character vector: features to plot}

\item{nrow}{number: no of rows in plot}
}
\value{
ggplot object
}
\description{
Plot volcano
}
\examples{
require(magrittr)
file <- download_data('fukuda20.proteingroups.txt')
object <- read_proteingroups(file, impute = TRUE)
object \%<>\% fit_limma()
plot_volcano(object)
plot_volcano(object, label = 'genesymbol')
plot_volcano(object, label = 'genesymbol', size = 'log2.LFQ.intensity')
plot_volcano(object, label = 'genesymbol', size = 'log2.LFQ.intensity', alpha = 'pepcounts')
plot_volcano(object, label = 'genesymbol', features = c('hmbsb'))
object \%<>\% fit_lm()

file <- download_data('atkin18.metabolon.xlsx')
object <- read_metabolon(file, impute = TRUE, fit = 'limma', plot = FALSE)
plot_volcano(object, coef = c('t1', 't2', 't3'))
object \%<>\% fit_lm(subgroupvar = 'SET')
plot_volcano(object, coef = c('t1', 't2', 't3'), nrow=2)
plot_volcano(object, coef = c('t1', 't2', 't3'), fit='lm')
}
