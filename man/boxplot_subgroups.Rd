% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/4_plot.R
\name{boxplot_samples}
\alias{boxplot_samples}
\alias{plot_sample_boxplots}
\alias{boxplot_features}
\alias{plot_feature_boxplots}
\alias{boxplot_subgroups}
\alias{boxplot_subgroups_per_coef}
\title{Boxplot samplegroups}
\usage{
boxplot_samples(
  object,
  assay = assayNames(object)[1],
  x = "sample_id",
  fill = "sample_id",
  color = NULL,
  highlight = NULL,
  pointsize = 0,
  jitter = 0.1,
  palette = NULL,
  n = 100,
  facet = NULL,
  scales = "free_x",
  nrow = NULL,
  ncol = NULL,
  page = 1,
  labeller = "label_value"
)

plot_sample_boxplots(...)

boxplot_features(
  object,
  assay = assayNames(object)[1],
  x = "feature_id",
  fill = "feature_id",
  color = NULL,
  highlight = NULL,
  pointsize = FALSE,
  jitter = 0.1,
  palette = NULL,
  n = 9,
  facet = NULL,
  scales = "free_y",
  nrow = NULL,
  ncol = NULL,
  page = 1,
  labeller = "label_value"
)

plot_feature_boxplots(...)

boxplot_subgroups(
  object,
  assay = assayNames(object)[1],
  x = "subgroup",
  fill = x,
  block = NULL,
  fit = fits(object)[1],
  coef = setdiff(coefs(object), "Intercept")[1],
  label = "feature_id",
  facet = c(label, pvar(object, coef, fit), fdrvar(object, coef, fit)),
  fdr = 1,
  pointsize = if (is.null(block)) 0 else 0.5,
  jitter = if (is.null(block)) 0.1 else 0,
  palette = NULL,
  title = coef,
  ylab = NULL,
  nrow = NULL,
  ncol = NULL,
  ntop = 4,
  labeller = "label_value",
  scales = "free_y",
  theme = NULL
)

boxplot_subgroups_per_coef(
  object,
  x = "subgroup",
  block = NULL,
  coefs = autonomics::coefs(object),
  title = coefs,
  nrow = 1,
  ncol = NULL,
  theme = ggplot2::theme(legend.position = "bottom", legend.title = element_blank(),
    plot.title = element_text(hjust = 0.05))
)
}
\arguments{
\item{object}{SummarizedExperiment}

\item{assay}{string}

\item{x}{svar (string)}

\item{fill}{svar (string)}

\item{highlight}{fvar expressing which feature should be highlighted (string)}

\item{pointsize}{number}

\item{jitter}{jitter width (number)}

\item{palette}{color palette (named character vector)}

\item{facet}{fvars on which to facet}

\item{scales}{'free', 'free_x', 'free_y', 'fixed'}

\item{nrow}{number}

\item{ncol}{number}

\item{page}{number of facet pages: \code{\link[ggforce]{facet_wrap_paginate}}}

\item{labeller}{string or function}

\item{block}{svar on which points are connected (string)}

\item{fit}{'limma', 'lm', 'lme', 'lmer', 'wilcoxon'}

\item{coef}{string}

\item{label}{label fvar}

\item{title}{string}

\item{ylab}{string}

\item{ntop}{number}

\item{theme}{ggplot2::theme() construct (or NULL)}

\item{fdrcutoff}{number}

\item{hlevels}{xlevels for which to plot horizontal lines}
}
\value{
ggplot object
}
\description{
Boxplot samplegroups
}
\examples{
# Read
    require(magrittr)
    file <- download_data('atkin18.metabolon.xlsx')
    object <- read_metabolon(file, plot = FALSE, fit = 'limma')
    controlfeatures <- c('biotin','phosphate')
    fdata(object) \%<>\% cbind(control = .$feature_name \%in\% controlfeatures)
# Boxplot
    boxplot_features(object[1:9, ])
    boxplot_samples(object[, 1:12])
    boxplot_samples(object[, 1:12], highlight = 'control')
    boxplot_subgroups(object, x = 'SET')
    boxplot_subgroups(object, x = 'SET', block = 'SUB')
    boxplot_subgroups_per_coef(object, x = 'SET', block = 'SUB')
}
\seealso{
\code{\link{plot_sample_densities}},
         \code{\link{plot_sample_violins}}
}
