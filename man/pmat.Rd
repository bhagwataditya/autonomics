% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/8_fit.R
\name{tvar}
\alias{tvar}
\alias{tmat}
\alias{pvar}
\alias{pmat}
\alias{p}
\alias{effectvar}
\alias{effectmat}
\alias{effect}
\alias{effectsize}
\alias{fdrvar}
\alias{fdrmat}
\alias{fdr}
\alias{up}
\alias{down}
\title{Get p/fdr/effect/down/up/sign matrix}
\usage{
tvar(object, coefs = autonomics::coefs(object), fit = fits(object))

tmat(object, coefs = autonomics::coefs(object), fit = fits(object))

pvar(object, coefs = autonomics::coefs(object), fit = fits(object))

pmat(object, coefs = autonomics::coefs(object), fit = fits(object))

p(...)

effectvar(object, coefs = autonomics::coefs(object), fit = fits(object))

effectmat(object, coefs = autonomics::coefs(object), fit = fits(object))

effect(...)

effectsize(object, coefs = autonomics::coefs(object), fit = fits(object))

fdrvar(object, coefs = autonomics::coefs(object), fit = fits(object))

fdrmat(object, coefs = autonomics::coefs(object), fit = fits(object))

fdr(...)

up(
  object,
  coefs = autonomics::coefs(object),
  fit = fits(object),
  var = "fdrmat",
  cutoff = 0.05
)

down(
  object,
  coefs = autonomics::coefs(object),
  fit = fits(object),
  var = "fdrmat",
  cutoff = 0.05
)
}
\arguments{
\item{object}{SummarizedExperiment}

\item{fit}{string}

\item{coef}{string}
}
\value{
matrix (feature x coef)
}
\description{
Get p/fdr/effect/down/up/sign matrix
}
\examples{
# Read
    file <- download_data('atkin18.metabolon.xlsx')
    object <- read_metabolon(file)
    object \%<>\% fit_limma()
    object \%<>\% fit_lm()
# Variables
    tvar(     object)
    pvar(     object)
    fdrvar(   object)
    effectvar(object)
# Matrix
    tmat(     object)[1:3, ]
    pmat(     object)[1:3, ]
    fdrmat(   object)[1:3, ]
    effectmat(object)[1:3, ]
    down(     object)[1:3, ]
    up(       object)[1:3, ]
    sign(     object)[1:3, ]
}
