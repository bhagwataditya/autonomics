% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/8_fit.R
\name{tvar}
\alias{tvar}
\alias{tmat}
\alias{pvar}
\alias{pmat}
\alias{p}
\alias{effectvar}
\alias{effectmat}
\alias{effect}
\alias{effectsizemat}
\alias{effectsize}
\alias{fdrvar}
\alias{fdrmat}
\alias{fdr}
\alias{upmat}
\alias{up}
\alias{downmat}
\alias{down}
\alias{sign.SummarizedExperimentz}
\title{Get p/fdr/effect/down/up/sign matrix}
\usage{
tvar(object, coefs = autonomics::coefs(object), fit = fits(object))

tmat(object, coefs = autonomics::coefs(object), fit = fits(object))

pvar(object, coefs = autonomics::coefs(object), fit = fits(object))

pmat(object, coefs = autonomics::coefs(object), fit = fits(object))

p(...)

effectvar(object, coefs = autonomics::coefs(object), fit = fits(object))

effectmat(object, coefs = autonomics::coefs(object), fit = fits(object))

effect(...)

effectsizemat(object, coefs = autonomics::coefs(object), fit = fits(object))

effectsize(...)

fdrvar(object, coefs = autonomics::coefs(object), fit = fits(object))

fdrmat(object, coefs = autonomics::coefs(object), fit = fits(object))

fdr(...)

upmat(
  object,
  coefs = autonomics::coefs(object),
  fit = fits(object),
  var = "fdrmat",
  cutoff = 0.05
)

up(...)

downmat(
  object,
  coefs = autonomics::coefs(object),
  fit = fits(object),
  var = "fdrmat",
  cutoff = 0.05
)

down(...)

\method{sign}{SummarizedExperimentz}(...)
}
\arguments{
\item{object}{SummarizedExperiment}

\item{fit}{string}

\item{coef}{string}
}
\value{
matrix (feature x coef)
}
\description{
Get p/fdr/effect/down/up/sign matrix
}
\examples{
# Read
    file <- download_data('atkin18.metabolon.xlsx')
    object <- read_metabolon(file)
    object \%<>\% fit_limma()
    object \%<>\% fit_lm()
# Variables
    tvar(     object)
    pvar(     object)
    fdrvar(   object)
    effectvar(object)
# Matrix
    tmat(     object)[1:3, ]
    pmat(     object)[1:3, ]
    fdrmat(   object)[1:3, ]
    effectmat(object)[1:3, ]
    downmat(  object)[1:3, ]
    upmat(    object)[1:3, ]
    signmat(  object)[1:3, ]
}
