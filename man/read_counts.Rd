% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/2_read_rnaseq.R
\name{read_counts}
\alias{read_counts}
\title{Read RNAseq counts}
\usage{
read_counts(
  file,
  fid_col = 1,
  fname_col = character(0),
  filter_count = 10,
  colfile = NULL,
  by.x = "sample_id",
  by.y = "sample_id",
  formula = if (single_subgroup(object)) ~1 else ~0 + subgroup,
  contrastdefs = contrast_subgroups(object),
  verbose = TRUE,
  plot = TRUE
)
}
\arguments{
\item{file}{string: path to rnaseq counts file}

\item{fid_col}{number of name of column with feature identifiers}

\item{fname_col}{string or number: feature name variable}

\item{filter_count}{number (default 10): filter out features
with less than 10 counts (in the smallest library) across samples. Filtering
performed with \code{\link[edgeR]{filterByExpr}}}

\item{colfile}{coldata file (string) or NULL}

\item{by.x}{merge var in file}

\item{by.y}{merge var in colfile}

\item{formula}{formula to create design matrix (using svars)}

\item{contrastdefs}{contrastdef vector/matrix/list}

\item{verbose}{TRUE/FALSE}

\item{plot}{TRUE/FALSE}
}
\value{
SummarizedExperiment
}
\description{
Read tsv file with rnaseq counts into SummarizedExperiment
}
\details{
File format: header row
             feature annotations in first few columns
             feature counts      in next columns
}
\examples{
file <- download_data('billing16.rnacounts.txt')
read_counts(file)

file <- download_data('billing19.rnacounts.txt')
read_counts(file)
}
\seealso{
merge_coldata, merge_rowdata
}
