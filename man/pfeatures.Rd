% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/8_fit.R
\name{pfeatures}
\alias{pfeatures}
\alias{upfeatures}
\alias{downfeatures}
\title{Get p/up/down features}
\usage{
pfeatures(
  object,
  fit = fits(object)[1],
  coef = default_coefs(object, fit = fit)[1],
  fvar = "feature_id",
  p = 0.05
)

upfeatures(
  object,
  fit = fits(object)[1],
  coef = default_coefs(object, fit = fit)[1],
  fvar = "feature_id",
  p = 0.05,
  effect = 0
)

downfeatures(
  object,
  fit = fits(object)[1],
  coef = default_coefs(object, fit = fit)[1],
  fvar = "feature_id",
  p = 0.05,
  effect = 0
)
}
\arguments{
\item{object}{SummarizedExperiment}

\item{fit}{string: subset of  fits(object)}

\item{coef}{string: subset of coefs(object)}

\item{fvar}{string: vector to be outputted}

\item{p}{pvalue cutoff}

\item{effect}{effect cutoff}
}
\value{
character vector
}
\description{
Get p/up/down features
}
\examples{
file <- download_data('atkin.metabolon.xlsx')
object <- read_metabolon(file)
object \%<>\% fit_limma()
   pfeatures(object)[1:3]
  upfeatures(object)[1:3]
downfeatures(object)[1:3]
}
