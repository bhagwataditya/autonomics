% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/4_plot.R
\name{filter_coefficient_features}
\alias{filter_coefficient_features}
\title{Extract coef features}
\usage{
filter_coefficient_features(
  object,
  fit = fits(object)[1],
  coef = default_coef(object, fit = fit),
  effectsize = 0,
  p = 1,
  fdr = 1,
  ntop = Inf
)
}
\arguments{
\item{object}{SummarizedXExperiment}

\item{fit}{string}

\item{coef}{string}

\item{effectsize}{effectsize cutoff}

\item{p}{p cutoff}

\item{fdr}{fdr cutoff}

\item{ntop}{ntop cutoff}
}
\description{
Extract coef features
}
\examples{
file <- download_data('fukuda20.proteingroups.txt')
object <- read_proteingroups(file, fit = 'limma')
nrow(object)                                    # 4534 features
nrow(filter_coefficient_features(object))             # 3772   p != NA
nrow(filter_coefficient_features(object,   p = 0.05)) # 1961   p < 0.05 
nrow(filter_coefficient_features(object, fdr = 0.05)) # 1622 fdr < 0.05 
}
