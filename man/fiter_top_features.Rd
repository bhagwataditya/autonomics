% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/4_plot.R
\name{fiter_top_features}
\alias{fiter_top_features}
\title{Filter coefficient features}
\usage{
fiter_top_features(
  object,
  fit = fits(object)[1],
  coef = default_coefficient(object, fit = fit),
  effectsize = 0,
  p = 1,
  fdr = 1,
  ntop = Inf
)
}
\arguments{
\item{object}{SummarizedXExperiment}

\item{fit}{string}

\item{coef}{string}

\item{effectsize}{effectsize cutoff}

\item{p}{p cutoff}

\item{fdr}{fdr cutoff}

\item{ntop}{ntop cutoff}
}
\description{
Filter coefficient features
}
\examples{
file <- download_data('fukuda20.proteingroups.txt')
object <- read_proteingroups(file, fit = 'limma')
nrow(object)                                    # 4534 features
nrow(fiter_top_features(object))             # 3772   p != NA
nrow(fiter_top_features(object,   p = 0.05)) # 1961   p < 0.05 
nrow(fiter_top_features(object, fdr = 0.05)) # 1622 fdr < 0.05 
}
