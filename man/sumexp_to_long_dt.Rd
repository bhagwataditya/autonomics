% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/3_sumexp_to_dt.R
\name{sumexp_to_wide_dt}
\alias{sumexp_to_wide_dt}
\alias{sumexp_to_long_dt}
\alias{sumexp_to_subrep_dt}
\title{Convert SummarizedExperiment into data.table}
\usage{
sumexp_to_wide_dt(
  object,
  fid = "feature_id",
  fvars = character(0),
  assay = "exprs"
)

sumexp_to_long_dt(
  object,
  fid = "feature_id",
  fvars = character(0),
  sid = "sample_id",
  svars = if ("subgroup" \%in\% importomics::svars(object)) {     "subgroup" } else {  
      character(0) },
  assay = "exprs"
)

sumexp_to_subrep_dt(object)
}
\arguments{
\item{object}{sumexp}

\item{fid}{fvar carrying feature id}

\item{fvars}{additional fvars to include in table}

\item{assay}{matrix in assays(object) to be used}

\item{sid}{svar carrying sample id}

\item{svars}{additional svars to include in table}
}
\value{
data.table
}
\description{
Convert SummarizedExperiment into data.table
}
\details{
\itemize{
   \item \code{sumexp_to_wide_dt}:   feature          x sample
   \item \code{sumexp_to_subrep_dt}: feature.subgroup x replicate
   \item \code{sumexp_to_long_dt}:   feature.sample
}
}
\examples{
# Stem cell comparison
    file <- download_data('billing16.proteingroups.txt')
    object <- read_proteingroups(file)
    sumexp_to_wide_dt(object)
    sumexp_to_long_dt(object)
    sumexp_to_subrep_dt(object)

# Glutaminase
   require(magrittr)
   file <- download_data('halama18.metabolon.xlsx')
   object <- read_metabolon(file)
   object \%<>\% pca()
   sumexp_to_wide_dt(object)
   sumexp_to_long_dt(object)
   sumexp_to_subrep_dt(object)
}
