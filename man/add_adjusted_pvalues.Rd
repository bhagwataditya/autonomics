% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/8_plot_volcano.R
\name{add_adjusted_pvalues}
\alias{add_adjusted_pvalues}
\title{Add adjusted pvalues}
\usage{
add_adjusted_pvalues(
  featuredt,
  method,
  sep = FITSEP,
  fit = names(featuredt) \%>\% extract(stri_detect_regex(., sprintf("^p[\%s]", sep)))
    \%>\% split_extract_fixed(sep, 3) \%>\% unique(),
  coefs = names(featuredt) \%>\% extract(stri_detect_regex(., sprintf("^p[\%s]", sep)))
    \%>\% split_extract_fixed(sep, 2) \%>\% unique()
)
}
\arguments{
\item{featuredt}{fdt(object)}

\item{method}{'fdr', 'bonferroni', ... (see `p.adjust.methods`)}

\item{sep}{string}

\item{fit}{'limma', 'lm', 'lme', 'lmer'}

\item{coefs}{coefficient (string)}
}
\value{
SummarizedExperiment
}
\description{
Add adjusted pvalues
}
\examples{
file <- download_data('fukuda20.proteingroups.txt')
object <- read_maxquant_proteingroups(file)
object \%<>\% fit_limma()
object \%<>\% extract(order(fdt(.)$`p~Adult~limma`), )
fdt(object)
fdt(object) \%>\% add_adjusted_pvalues('fdr')
fdt(object) \%>\% add_adjusted_pvalues('bonferroni')
}
